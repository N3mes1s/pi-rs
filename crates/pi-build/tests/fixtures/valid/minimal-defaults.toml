# Manifest exercising every default — no [secrets], no [tools],
# no runtime.max_* fields. Per A.8 "defaults applied" test.
schema_version = 1

[agent]
name        = "minimal"
description = "All defaults."
version     = "0.0.1"

[provider]
name  = "anthropic"
model = "claude-haiku-4-5-20251001"

[runtime]
system_prompt = "Hello."
