# Fast route examples — trivial diffs, renames, typo fixes, single-line edits,
# low-cognitive-load asks. One example per line; comment lines (# ...) ignored.
#
# Authoring technique: 10 canonical seeds × 10 paraphrase surface forms each.
# Vocabulary diversity matters for cosine discrimination — paraphrase surface
# forms rather than using synonyms of the same nouns.

# Seed 1: variable / symbol rename in one file
rename foo to bar in this file
change the variable name from foo to bar throughout src/lib.rs
update identifier foobar to baz across this single module
swap the function name from old_name to new_name in main.rs
rename the constant MAX_SIZE to BUFFER_LIMIT in config.rs
replace every occurrence of old_var with new_var in this file only
update the struct field name from `data` to `payload` in models.rs
rename the enum variant Pending to Queued across this crate
change the label `err` to `e` for consistency in util.rs
rename parameter `input` to `buf` in the function signature

# Seed 2: remove a debug statement or dead line
remove the println at line 42
delete the debug eprintln on line 17
take out the dbg! macro call added temporarily
strip the leftover print statement from the top of main
remove the TODO comment at line 88
delete the commented-out block on lines 30 to 35
erase the unused let binding on line 50
remove the leftover todo!() placeholder in handlers.rs
strip the extra blank lines in the middle of the function
delete the dead else branch that's unreachable

# Seed 3: add a doc comment to a single item
add a doc comment to this function
write a brief doc string for this struct
add a /// comment explaining what this enum variant means
put a one-line doc on top of this public constant
add a missing doc comment to the trait method
write a short rustdoc for this module at the top of the file
add a doc comment to the impl block describing the purpose
put a one-line summary doc above this type alias
add a short rustdoc explaining the return value
annotate this public field with a brief doc comment

# Seed 4: describe what a diff or file does
just describe the diff
what does this diff change
give a plain English summary of this patch
explain what changed in this commit
describe what the code in this file does at a high level
tell me what this function is supposed to return
briefly explain what this module exports
what does this constant do
what is the purpose of this struct field
summarize this file in two sentences

# Seed 5: delete an unused import or dead code
delete an unused import
remove the dead import at the top
clean up unused use statements in this file
strip the import that triggers an unused warning
take out the dead use statement for std::io
remove the import for rand that's no longer needed
delete the unused extern crate declaration
trim the no-longer-needed import of Vec at the top
strip the duplicate import of HashMap
remove the leftover use crate::util line that is dead

# Seed 6: fix a typo in identifier, comment, or string literal
fix this typo in the README
correct the spelling mistake in the comment
fix the typo in the string literal on line 12
correct the identifier `recieve` to `receive` everywhere in the file
fix the misspelled function name `desrialize` to `deserialize`
correct the doc comment typo "occured" to "occurred"
repair the typo in the error message string
fix the misspelling `seperate` in this module
correct "retreive" to "retrieve" in the function name
update the comment spelling from "thier" to "their"

# Seed 7: single-line mechanical edit (bump version, change a literal)
bump the version in Cargo.toml from 0.1.0 to 0.2.0
update the default timeout constant from 30 to 60
change the hard-coded port number from 8080 to 3000
set the default retry count from 3 to 5 in config.rs
update the feature flag name from old_feature to new_feature
change the log level in this call from info to debug
update the copyright year in the license header
bump the minimum Rust edition from 2021 to 2024 in Cargo.toml
change the default stack size constant from 4096 to 8192
update the hash seed literal from 42 to 137 in the implementation

# Seed 8: is a symbol public / what is its visibility
is Z public
what is the visibility of this function
is this struct exported from the crate
which methods on this type are pub
is this field accessible outside the module
does this module re-export the type
what traits does this struct derive
is this enum pub or pub(crate)
which symbols from this file are in the crate root
is this associated type visible externally

# Seed 9: format or whitespace cleanup
reformat this block to match rustfmt output
fix the indentation in this function body
run cargo fmt on this file and apply the result
align the match arms in this pattern
add a trailing comma to this struct literal
remove the trailing whitespace on line 20
fix the brace style to match the rest of the codebase
reformat the long line that exceeds 100 characters
break this long function signature across multiple lines
normalize the blank lines between methods to one

# Seed 10: describe whether two snippets are equivalent
are these two snippets equivalent
do these two expressions compute the same result
is this refactored version semantically identical to the original
does swapping these two lines change behavior
are these match arms covering the same cases
is this import the same as the one I removed
does this alternative impl do the same thing
are these two conditions logically equivalent
check whether the simplified version is the same
is this rewrite behavior-preserving for the happy path
