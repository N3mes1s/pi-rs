# Per-error fixture: ManifestError::InvalidAgentName.
# `agent.name` must match ^[a-z][a-z0-9_-]{0,63}$ — uppercase rejected.
schema_version = 1

[agent]
name        = "BadName"
description = "y"
version     = "0.1.0"

[provider]
name  = "anthropic"
model = "m"

[runtime]
system_prompt = "p"
